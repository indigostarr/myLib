<%- include("partials/header.ejs") %>

<div class="relative min-h-screen flex">
  <!-- sidebar -->
  <!-- text blue 100 will go all the way down the page -->
  <!-- this is the parent div so will set padding y  -->
  <div
    class="
      bg-gray-50
      text-black
      w-64
      space-y-6
      py-7
      px-2
      absolute
      inset-y-0
      left-0
      transform
      -translate-x-full
      md:relative
      md:translate-x-0
      transition
      duration-200
      ease-in-out
    "
  >
    <!-- logo -->
    <a
      href="/books"
      class="text-black flex-items-center space-x-2 px-4"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
      ><span class="w-8 h-8 text-2x1 font-bold">✨ MyLib</span>
    </a>
    <nav>
      <a
        href="/"
        class="
          block
          py-2.5
          px-4
          rounded
          transition
          duration-200
          hover:bg-gray-100
        "
        >🔦 Search</a
      >
      <a
        href="/books/to-read"
        class="
          block
          py-2.5
          px-4
          rounded
          transition
          duration-200
          hover:bg-gray-100
        "
        >📕 To Read</a
      >
      <a
        href="/books/reading"
        class="
          block
          py-2.5
          px-4
          rounded
          transition
          duration-200
          hover:bg-gray-100
        "
        >👀 Reading</a
      >
      <a
        href="/books/read"
        class="
          block
          py-2.5
          px-4
          rounded
          transition
          duration-200
          hover:bg-gray-100
        "
        >📚 Read</a
      >

      <!-- remove  abosolute and transform and -translate x-full-->
      <!-- md:relative 
      md:translate-x-0 these make the sidebar responsive -->
    </nav>
  </div>
  <div class="flex-1 p-10 text-2x1 font-bold">

    <!-- Create a display area to show the new books -->
    <div id="<%= book.id %>" name="<%= book.id %>">
      <div><%= book.status %></div>
    <form action="/books/<%= book.id %>" method="POST" id="book-review">
      <input name="_method" type="hidden" value="PUT" />
      
      <img id="header-image" src="<%= book.thumbnail %>" /><input
        type="hidden"
        name="thumbnail"
        value="<%= book.thumbnail %>"
      />
      <label
        ><input type="hidden" name="title" value="<%= book.title %>" /> <%=
        book.title %></label
      >
      <label><input type="hidden" name="author" value="<%= book.author %>"/>By <%= book.author %></label>
      <input type="radio" onchange='this.form.submit();' name="status" class="reading-status" id="to-read" value="to-read"  <% if (book.status === "to-read") { %>checked <% }%>>
        <label for="to-read" class="reading-status__label">📕 To Read</label>
        <input type="radio" onchange='this.form.submit();' name="status" class="reading-status" id="read" value="read" <% if (book.status === "read") { %>checked <% }%>><label for="read" class="reading-status__label">📚 Read</label>
        <input type="radio" onchange='this.form.submit();' name="status" class="reading-status"  id="reading" value="reading" <% if (book.status === "reading") { %>checked <% }%>><label for="reading" class="reading-status__label">👀 Reading</label>
      <label><%= book.pages %><input type="hidden" name="pages" /></label>
      <label><%= book.description %> <input type="hidden" name="description"
      value="<%= book.description %>"/></label>
      <input
        type="text"
        name="review"
        id="review-textarea"
        style="size: 2000; width: 80%; height: 150px"
        value="<%= book.review %>"
      />
      <a href="/books/delete/<%= book.id %>">Won't Read</a></td> 
    </form>
  </div>
</div>
</div>
  <%- include("partials/footer.ejs") %>

